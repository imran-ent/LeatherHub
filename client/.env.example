# Client env — set only if API is NOT on same host (e.g. Vercel + Render split deploy)
# If your API is at https://leather-hub-api.onrender.com then:
# VITE_API_URL=https://leather-hub-api.onrender.com
# Leave empty when serving client from the same Express server (single-host deploy)
VITE_API_URL=
